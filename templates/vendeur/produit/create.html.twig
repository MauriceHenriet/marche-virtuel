{% extends "vendeur/base_vendeur.html.twig" %}

{% block title %}creer produit{% endblock %}


{% block body %}

    <section class="main-acheteur">

        <div class="card-header boutique-up">
            <h3> Ajouter un Produit</h3>
            <a href="{{ path('show_boutique', { 'id' : boutiqueId }) }}" class="btn btn-warning">
                <i class="fas fa-arrow-left"></i> Retour
            </a>
        </div>

        <div class="car-body section-down form-boutique">
            {{ form_start(formProduit) }}

            {{ form_widget(formProduit) }}
                <button class="btn btn-primary mt-3">Enregistrer</button>
            {{ form_end(formProduit) }}
        </div>

    </section>

{% endblock %}