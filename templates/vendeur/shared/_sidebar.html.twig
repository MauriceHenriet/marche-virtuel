<div class="sideBarAdmin">
    <div>
        <a href="{{ path('vendeur_home') }}" class="col-12 btn btn-success mt-3">
            <i class="fas fa-store"></i> Boutiques
        </a>
    </div>
    <div>
        <a href="{{ path('vendeur_commandes_show') }}" class="col-12 btn btn-primary mt-3">
            <i class="fas fa-users">
            {% if nbCommandesATraiter > 0 %}
                <span style="color:red ; font-size:20px"> {{ nbCommandesATraiter }}</span>
            {% endif %}
            </i> Commandes
        </a>
    </div>
    <div>
        <a href="{{ path('profile_personnel') }}" class="col-12 btn btn-primary mt-3"> Espace Acheteur </a>
    </div>
    <div>
        <a href="{{ path('public_home') }}" class="col-12 btn btn-primary mt-3"> Retour au site </a>
    </div>
    <div>
        <a href="{{ path('app_logout') }}" class="col-12 btn btn-danger mt-3"> Déconnexion </a>
    </div>

</div>