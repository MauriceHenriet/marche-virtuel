<div class="vMobile">
    <nav id="the-nav" class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="{{ path('profile_personnel') }}">
                <i class="fas fa-store"></i> Espace Acheteur
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor02" aria-controls="navbarColor02" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarColor02">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a href="{{ path('profile_commandes_show') }}" class="nav-link">
                            <i class="fas fa-users">
                            {% if nbCommandesAttente > 0 %}
                                <span style="color:red ; font-size:20px"> {{ nbCommandesAttente }}</span>
                            {% endif %}
                            
                            </i> Commandes
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('cart_show')}}" class="nav-link">
                            <i class="fas fa-shopping-cart">
                            {% if nbArticles > 0 %}
                                <span style="color:orange ; font-size:20px"> {{ nbArticles }}</span>
                            {% endif %}
                            </i> Panier
                        </a>
                    </li>
                    <li class="nav-item">
                        {% if "ROLE_VENDEUR" in app.user.roles %}
                            <a href="{{ path('vendeur_home') }}" class="nav-link"> Espace Vendeur </a>
                        {% endif %}
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('public_home') }}" class="nav-link"> Retour au site </a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('app_logout') }}" class="nav-link"> Déconnexion </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</div>